{
  "name": "express_middleware_proxy",
  "version": "1.0.0",
  "description": "Small test harness for experimenting with proxy solutions, using Express Middleware.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "latest",
    "express": "latest",
  }
}